
<div>
    <div class="grid">

        <div class="form-control">
        <label class="label">
            <span class="label-text">Enter name</span>
        </label>
        <form class="input-group" method="POST">
            <input id="name" type="text" name="name" placeholder="Name" class="input input-bordered" />
            <span><button type="submit" class="btn btn-primary" Name="ADD" onclick="AddElement(this)">Add</button></span>
        </form>
        </div>
    </div>
</div>


<hr class="my-4" style="height: 30px; width: auto;">

<div id="field">

{% if users1 %}


    {% for id, name in users1 %}
    <div class="mb-4">
        <div class="grid">
            <div class="form-control">
            <form class="input-group" method="POST">
                <input type="name" name="name" placeholder="{{name}}" value="{{name}}" class="input input-bordered" />
                <span><button type="submit" class="btn btn-primary" Name="EDIT" onclick="EditElement('{{name}}', this)">save</button></span>
                <span><button type="submit" class="btn btn-error"Name="DELETE" onclick="DeleteElement(this);">delete</button></span>
                <input id="oldname" name="oldname" style="visibility: hidden;" value="{{name}}"/>
                <input id="id" name="id" style="visibility: hidden;" value="{{id}}"/>
            </form>
            </div>
        </div>
    </div>

    {% endfor %}

{% endif %}
</div>


<!--

<div>
    <div class="grid">
        <div class="form-control">
        <label class="input-group">
            <input type="text" placeholder="Name" class="input input-bordered" />
            <span><button class="btn btn-primary">save</button></span>
            <span><button class="btn btn-error">delete</button></span>
        </label>
        </div>
    </div>
</div>

-->


<script>


//import { DocumentStore } from 'ravendb';

//console.log(raven);

function LoadAll()
{
    //let field = document.getElementById('field');
    let users = getAllUserNames();
    for(let i = 0; i < users.length; i++)
    {

    }
}

function AddElement(nameObj)
{
    let name = document.getElementById('name').value;
    //console.log(name);

}

function EditElement(was, nameObj)
{
    let name = nameObj.parentElement.parentElement.children[0].value;
    console.log(name);
}

function DeleteElement(nameObj)
{
    let name = nameObj.parentElement.parentElement.children[0].placeholder;
    
}

</script>